# Logicoin v0.12.15.3 — Node Asset Registry Startup Hotfix

- Fixed the Windows `dist` package missing `logicoin_public_network.json`.
- Added the registry JSON as PyInstaller data for both Control Center and Node.
- Added a safe embedded fallback registry.
- Added clearer node startup diagnostics with the node-log tail.
- Wrapped token and bridge remain disabled.
